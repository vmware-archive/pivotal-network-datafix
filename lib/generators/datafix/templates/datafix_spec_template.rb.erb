require Rails.root.join("db", "datafixes", "<%= "#{timestamp}_#{file_name}" %>")

describe Datafixes::<%= file_name.camelize %> do
  describe ".up" do
    pending "Needs tests"
  end

  describe ".down" do
    pending "Needs tests"
  end
end
